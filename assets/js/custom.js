// import fsFileTree from 'fs-file-tree';

// console.log(fsFileTree.sync('../images', {camelCase: true}));


$(document).ready(function(){
/*     var newimg = $('.camview');
    function getExif() {
        $.each(newimg, function(i, item){
            EXIF.getData(item, function () {
                var orientation = EXIF.getTag(item, "Orientation");
                if($(item).hasClass('rotate90')||$(item).hasClass('rotate180')||$(item).hasClass('rotate270')) {
                    return false;
                } else if (orientation == 6) {
                    console.log('orientation is 6')
                    $(item).addClass("camview rotate90");
                } else if (orientation == 3) {
                    console.log('orientation is 3')
                    $(item).addClass("camview rotate180");
                } else if (orientation == 8) {
                    console.log('orientation is 8')
                    $(item).addClass("camview rotate270");
                }
            });
        });
    };
    if(newimg.prop('complete')) {
        getExif();
    } else {
        newimg.on('load', function(){
            getExif();
        })
    } */
});

